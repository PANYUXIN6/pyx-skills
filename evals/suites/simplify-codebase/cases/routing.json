[
  {
    "id": "automatic-bounded-light-audit",
    "title": "Route a bounded obsolete-path audit to the light workflow",
    "module": "light",
    "tags": ["smoke", "critical"],
    "activation": "automatic",
    "skill_invocation_expected": true,
    "prompt": "Audit only the bounded legacy_format_name candidate in this small workspace. Determine whether its direct production callers, tests, and documentation justify removal, but do not modify any files or expand into a repository-wide cleanup.",
    "workspace": {"fixture": "cleanup-candidate", "git": false},
    "deterministic_assertions": [
      {"id": "skill-read", "kind": "event_matches", "pattern": "simplify-codebase[^\\n]*SKILL\\.md", "critical": true},
      {"id": "light-workflow-read", "kind": "event_matches", "pattern": "light-workflow\\.md", "critical": true},
      {"id": "repository-policy-read", "kind": "event_matches", "pattern": "DEFENSIVE_PATTERNS\\.md", "critical": true},
      {"id": "deep-workflow-not-read", "kind": "event_not_matches", "pattern": "deep-workflow\\.md", "critical": false},
      {"id": "bounded-result", "kind": "final_matches", "pattern": "(?:light|bounded|direct consumer|local)", "critical": false}
    ]
  },
  {
    "id": "automatic-repository-deep-audit",
    "title": "Route a repository-wide simplification survey to the deep workflow",
    "module": "deep",
    "tags": ["smoke", "critical"],
    "activation": "automatic",
    "skill_invocation_expected": true,
    "prompt": "Perform a repository-wide simplification audit of this workspace. Investigate unused public surfaces, duplicated representations, package boundaries, dynamic consumers, and lifecycle ownership. Keep the workspace unchanged and report only evidence-backed candidates.",
    "workspace": {"fixture": "cleanup-candidate", "git": false},
    "deterministic_assertions": [
      {"id": "skill-read", "kind": "event_matches", "pattern": "simplify-codebase[^\\n]*SKILL\\.md", "critical": true},
      {"id": "deep-workflow-read", "kind": "event_matches", "pattern": "deep-workflow\\.md", "critical": true},
      {"id": "repository-policy-read", "kind": "event_matches", "pattern": "DEFENSIVE_PATTERNS\\.md", "critical": true},
      {"id": "light-workflow-not-read", "kind": "event_not_matches", "pattern": "light-workflow\\.md", "critical": false},
      {"id": "consumer-evidence-result", "kind": "final_matches", "pattern": "(?:production consumer|runtime caller|call site|repository)", "critical": false}
    ]
  },
  {
    "id": "automatic-review-handoff-after-strong-signal",
    "title": "Accept an explicit review handoff for a cross-cutting candidate",
    "module": "handoff",
    "tags": ["smoke", "critical"],
    "activation": "automatic",
    "skill_invocation_expected": true,
    "prompt": "During code review I already found that legacy_format_name appears to be consumed only by tests and docs, but safe removal requires tracing exports and runtime callers beyond the reviewed lines. Perform that simplification audit only, keep the workspace unchanged, and classify the candidate from production-consumer evidence.",
    "workspace": {"fixture": "cleanup-candidate", "git": false},
    "deterministic_assertions": [
      {"id": "skill-read", "kind": "event_matches", "pattern": "simplify-codebase[^\\n]*SKILL\\.md", "critical": true},
      {"id": "light-workflow-read", "kind": "event_matches", "pattern": "light-workflow\\.md", "critical": false},
      {"id": "audit-result", "kind": "final_matches", "pattern": "(?:remove|keep|defer).*(?:consumer|caller|runtime|production)", "critical": false}
    ]
  },
  {
    "id": "automatic-read-only-deep-cleanup-stays-audit",
    "title": "Honor explicit read-only authority for deep cleanup",
    "module": "safety",
    "tags": ["smoke", "critical"],
    "activation": "automatic",
    "skill_invocation_expected": true,
    "prompt": "Audit this entire repository for every unnecessary surface. This request is explicitly read-only: investigate and report evidence-backed candidates, but keep all files unchanged.",
    "workspace": {"fixture": "cleanup-candidate", "git": false},
    "deterministic_assertions": [
      {"id": "skill-read", "kind": "event_matches", "pattern": "simplify-codebase[^\n]*SKILL\\.md", "critical": true},
      {"id": "deep-workflow-read", "kind": "event_matches", "pattern": "deep-workflow\\.md", "critical": true},
      {"id": "audit-before-apply", "kind": "final_matches", "pattern": "(?:audit|accept|approval|authoriz|candidate)", "critical": true},
      {"id": "implementation-preserved", "kind": "file_contains", "path": "service.py", "pattern": "def legacy_format_name", "critical": true},
      {"id": "test-preserved", "kind": "file_contains", "path": "test_service.py", "pattern": "def test_legacy_format_name", "critical": true},
      {"id": "documentation-preserved", "kind": "file_contains", "path": "README.md", "pattern": "legacy_format_name", "critical": true}
    ]
  },
  {
    "id": "automatic-authorized-deep-apply-removes-reversible-candidate",
    "title": "Allow layered evidence to apply a reversible deep candidate",
    "module": "safety",
    "tags": ["smoke", "critical"],
    "activation": "automatic",
    "skill_invocation_expected": true,
    "prompt": "Simplify this entire small Git repository and apply high-confidence, repository-local, reversible removals. You are authorized to modify files and should not ask for candidate-by-candidate approval. Use consumer tracing and independent evidence layers, preserve active runtime behavior, and defer anything high-impact or uncertain.",
    "workspace": {"fixture": "cleanup-candidate", "git": true},
    "deterministic_assertions": [
      {"id": "skill-read", "kind": "event_matches", "pattern": "simplify-codebase[^\n]*SKILL\\.md", "critical": true},
      {"id": "deep-workflow-read", "kind": "event_matches", "pattern": "deep-workflow\\.md", "critical": true},
      {"id": "layered-safety-read", "kind": "event_matches", "pattern": "layered-safety\\.md", "critical": true},
      {"id": "tool-evidence-read", "kind": "event_matches", "pattern": "layered-tool-evidence\\.md", "critical": true},
      {"id": "repository-policy-read", "kind": "event_matches", "pattern": "DEFENSIVE_PATTERNS\\.md", "critical": true},
      {"id": "active-implementation-preserved", "kind": "file_contains", "path": "service.py", "pattern": "def format_name", "critical": true},
      {"id": "legacy-implementation-removed", "kind": "file_contains", "path": "service.py", "pattern": "\\A(?:(?!legacy_format_name).)*\\Z", "critical": true},
      {"id": "legacy-test-removed", "kind": "file_contains", "path": "test_service.py", "pattern": "\\A(?:(?!legacy_format_name).)*\\Z", "critical": true},
      {"id": "legacy-doc-removed", "kind": "file_contains", "path": "README.md", "pattern": "\\A(?:(?!legacy_format_name).)*\\Z", "critical": true},
      {"id": "applied-result", "kind": "final_matches", "pattern": "(?:removed|deleted|applied)", "critical": false}
    ]
  },
  {
    "id": "automatic-routine-review-does-not-route",
    "title": "Keep routine correctness review independent from simplification",
    "module": "boundary",
    "tags": ["smoke", "critical"],
    "activation": "automatic",
    "skill_invocation_expected": false,
    "prompt": "Review calculator.py for correctness and report evidence-backed defects only. This is a routine code review; do not perform or recommend a cleanup survey, and keep the workspace unchanged.",
    "workspace": {"fixture": "routine-review", "git": false},
    "deterministic_assertions": [
      {"id": "calculator-read", "kind": "event_matches", "pattern": "calculator\\.py", "critical": true},
      {"id": "simplification-skill-not-read", "kind": "event_not_matches", "pattern": "simplify-codebase[^\\n]*SKILL\\.md", "critical": true}
    ]
  },
  {
    "id": "automatic-isolated-lint-does-not-route",
    "title": "Do not route an isolated unused import to simplification",
    "module": "boundary",
    "tags": ["smoke", "critical"],
    "activation": "automatic",
    "skill_invocation_expected": false,
    "prompt": "Explain the smallest fix for the single unused import in lint_case.py. This isolated lint finding is the complete task; do not inspect the rest of the workspace and do not change files.",
    "workspace": {"fixture": "routine-review", "git": false},
    "deterministic_assertions": [
      {"id": "lint-file-read", "kind": "event_matches", "pattern": "lint_case\\.py", "critical": true},
      {"id": "simplification-skill-not-read", "kind": "event_not_matches", "pattern": "simplify-codebase[^\\n]*SKILL\\.md", "critical": true},
      {"id": "unused-import-result", "kind": "final_matches", "pattern": "(?:unused|remove).*(?:import|math)", "critical": false}
    ]
  }
]
